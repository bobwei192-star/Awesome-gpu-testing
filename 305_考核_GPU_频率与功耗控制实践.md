# 第305天：[考核] GPU 频率与功耗控制实践

## 学习目标
- 综合运用第 302‑304 天所学知识，完成频率调节、电压控制、功耗限制的实践考核
- 掌握通过 sysfs、debugfs、rocm‑smi 联合监控与调整 GPU 性能状态的方法
- 能够设计并执行完整的频率‑电压‑功耗协同优化实验，收集数据并分析结果
- 深入理解内核中频率‑功耗控制代码的交互逻辑，并能通过内核日志验证行为

## 考核结构
本考核分为四个部分，总分 100 分，通过标准为 70 分。

| 部分 | 内容 | 分值 | 时间建议 |
|------|------|------|---------|
| **第一部分**：理论知识测验 | 10 道选择题（单选/多选） | 20 分 | 30 分钟 |
| **第二部分**：实践操作任务 | 5 个实际命令行任务 | 30 分 | 60 分钟 |
| **第三部分**：综合实验设计 | 设计并执行一个优化实验 | 40 分 | 90 分钟 |
| **第四部分**：内核代码分析 | 分析关键函数调用流程 | 10 分 | 30 分钟 |

## 第一部分：理论知识测验（20 分）

### 1.1 单选题（每题 2 分，共 10 分）

**1. 关于 AMD GPU 的 SCLK（Shader Clock），以下描述正确的是：**
A) SCLK 只控制计算单元（CU）的运行频率，与显存无关
B) SCLK 与 MCLK（Memory Clock）总是同步调整，不能独立控制
C) SCLK 的调整会影响 GPU 核心的计算性能和功耗，但不会直接影响显存带宽
D) 在 RDNA 3 架构中，SCLK 的最大值由 GPU 的 VID（Voltage Identifier）单独决定，与温度无关

**2. AMD GPU 的 PPT（Package Power Limit）管理机制中，当实测功耗超过 PPT 限制时，最先发生的是：**
A) 立即关闭 GPU 以保护硬件
B) 降低 SCLK 频率以减少功耗
C) 降低核心电压（VDDC）以减少功耗
D) 触发温度保护机制，降低风扇转速

**3. 关于电压‑频率曲线（V/F Curve），错误的说法是：**
A) 电压‑频率曲线描述了在不同频率下 GPU 稳定运行所需的最低电压
B) 降低电压可以在相同频率下减少功耗，从而在固定 PPT 下获得更高性能
C) 电压‑频率曲线是线性的，频率加倍时电压也需要加倍
D) 温度升高会导致电压‑频率曲线右移，即相同频率需要更高电压

**4. 通过 sysfs hwmon 接口设置功耗限制时，正确的命令格式是：**
A) `echo "250W" > /sys/class/drm/card0/device/power_limit`
B) `echo 250 > /sys/class/drm/card0/device/hwmon/hwmon*/power_limit`
C) `echo 250000000 > /sys/class/drm/card0/device/hwmon/hwmon*/power1_cap`
D) `sudo set_power_limit 250 /sys/class/drm/card0/device`

**5. 在调试 GPU 频率调节问题时，最有效的内核日志查看方法是：**
A) 查看 `/var/log/syslog` 中的 amdgpu 相关条目
B) 使用 `dmesg | grep -i amdgpu` 查看内核环缓冲区
C) 直接读取 `/proc/gpu_freq_debug` 虚拟文件
D) 查看 `/sys/kernel/debug/dri/0/amdgpu_freq_info`

### 1.2 多选题（每题 2 分，共 10 分，全部选对得 2 分，漏选得 1 分，错选不得分）

**6. 以下哪些因素会影响 AMD GPU 达到的最高稳定频率？（选择所有正确的）**
A) GPU 核心温度
B) 当前设置的 PPT 限制
C) GPU 硅片体质（ASIC quality）
D) 操作系统类型（Windows vs Linux）
E) 显存容量大小

**7. 关于 rocm‑smi 工具，以下哪些说法是正确的？**
A) `rocm-smi --showpower` 可以显示当前功耗和功耗限制
B) `rocm-smi --setsclk 3` 将 SCLK 设置为第 3 级性能状态
C) `rocm-smi --resetfans` 可以将风扇控制恢复为自动模式
D) `rocm-smi --showallinfo` 包含电压、温度、功耗等全部信息
E) `rocm-smi` 需要 root 权限才能运行

**8. AMD GPU 电源管理涉及的 kernel 源码文件包括：（选择所有正确的）**
A) `drivers/gpu/drm/amd/pm/amdgpu_dpm.c`
B) `drivers/gpu/drm/amd/pm/swsmu/smu_v13_0.c`
C) `drivers/gpu/drm/amd/display/dc/dc_power.c`
D) `drivers/gpu/drm/amd/acpi/amdgpu_acpi.c`
E) `drivers/gpu/drm/amd/ttm/amdgpu_ttm.c`

**9. 当 GPU 发生热节流（Thermal Throttling）时，可能观察到的现象有：**
A) SCLK 频率动态降低又恢复
B) `/sys/class/drm/card0/device/hwmon/hwmon*/temp1_input` 温度读数超过阈值
C) 内核日志中出现 `amdgpu: thermal throttling activated` 消息
D) GPU 利用率突然降至 0%
E) 风扇转速急剧升高

**10. 优化 GPU 能效（Performance per Watt）的可行方法包括：**
A) 在性能损失可接受范围内适度降低 PPT
B) 使用 undervolting（降低电压）技术
C) 将 GPU 固定在高性能状态（high performance state）
D) 改善机箱散热以降低 GPU 工作温度
E) 禁用所有电源管理功能以保持频率稳定

---

## 第二部分：实践操作任务（30 分）

请在实际的 AMD GPU 系统上完成以下任务，并将命令和输出结果记录下来。如果无法访问实际硬件，请基于理论知识描述每一步骤的预期结果。

### 任务 2.1：频率与性能状态信息收集（6 分）

**目标**：全面收集 GPU 当前的频率、电压、功耗、温度信息。

**步骤**：
1. 使用 rocm‑smi 查看 GPU 概要信息。
2. 使用 rocm‑smi 的详细模式查看所有性能状态（Performance Level）信息。
3. 通过 sysfs hwmon 接口读取当前温度、功耗、功耗限制。
4. 通过 debugfs 查看更详细的电源管理信息。

**要求**：
- 提供完整的命令序列。
- 解释每个命令输出中的关键字段含义。
- 至少找出三个不同信息来源中关于 SCLK 频率的数据，并验证它们是否一致。

**示例命令框架**：
```bash
# 1. rocm-smi 概要
rocm-smi

# 2. 详细性能状态
rocm-smi --showperfinfo

# 3. hwmon 信息
ls /sys/class/drm/card0/device/hwmon/
cat /sys/class/drm/card0/device/hwmon/hwmon*/temp1_input
cat /sys/class/drm/card0/device/hwmon/hwmon*/power1_input
cat /sys/class/drm/card0/device/hwmon/hwmon*/power1_cap

# 4. debugfs 信息（需要 root）
sudo cat /sys/kernel/debug/dri/0/amdgpu_pm_info
```

### 任务 2.2：频率手动调节（6 分）

**目标**：通过 sysfs 接口手动调整 GPU 频率，观察频率变化及对功耗的影响。

**步骤**：
1. 将 GPU 设置为手动性能状态控制模式。
2. 将 SCLK 设置为一个较低的性能等级（如 level 2）。
3. 运行一个轻量级 GPU 负载（如 `glmark2 --run-forever &`）。
4. 监控此时的功耗和温度。
5. 将 SCLK 设置为较高的性能等级（如 level 5 或最高）。
6. 再次监控功耗和温度，比较差异。

**要求**：
- 提供完整的命令序列。
- 记录频率变化前后的功耗、温度数据。
- 分析频率提升对功耗的影响程度（例如：频率增加 X%，功耗增加 Y%）。

**关键命令提示**：
```bash
# 查看可用的性能状态
cat /sys/class/drm/card0/device/pp_od_clk_voltage

# 设置手动模式
echo "manual" > /sys/class/drm/card0/device/power_dpm_force_performance_level

# 设置 SCLK 等级（假设支持 0-7）
echo "s 2" > /sys/class/drm/card0/device/pp_od_clk_voltage
echo "c" > /sys/class/drm/card0/device/pp_od_clk_voltage
```

### 任务 2.3：功耗限制设置与验证（6 分）

**目标**：设置一个低于默认值的功耗限制，验证限制是否生效。

**步骤**：
1. 记录默认的功耗限制值。
2. 设置一个新的功耗限制（如默认值的 80%）。
3. 运行一个重负载测试（如 `glmark2` 或 `furmark`）。
4. 监控测试期间的功耗，确保不超过新设置的限制。
5. 观察是否因功耗限制而触发频率调整。

**要求**：
- 提供监控脚本或命令序列。
- 展示测试期间功耗随时间变化的趋势。
- 如果功耗超过限制，分析可能的原因。

**监控脚本框架**：
```bash
#!/bin/bash
# 设置功耗限制（示例：200W）
echo 200000000 > /sys/class/drm/card0/device/hwmon/hwmon*/power1_cap

# 运行监控循环
for i in {1..60}; do
    power=$(cat /sys/class/drm/card0/device/hwmon/hwmon*/power1_input)
    limit=$(cat /sys/class/drm/card0/device/hwmon/hwmon*/power1_cap)
    freq=$(cat /sys/class/drm/card0/device/pp_dpm_sclk | grep "*" | awk '{print $2}')
    echo "$i, $((power/1000000))W, $((limit/1000000))W, $freq MHz"
    sleep 1
done
```

### 任务 2.4：电压调节（Undervolting）实验（6 分）

**目标**：尝试降低 GPU 核心电压，观察对功耗和稳定性的影响。

**步骤**：
1. 查看当前的电压‑频率曲线。
2. 选择一个频率点（如 2000 MHz），尝试将其所需电压降低 50 mV。
3. 应用新的电压设置。
4. 运行稳定性测试（如 `glmark2` 或 `vkmark` 持续运行 5 分钟）。
5. 监控功耗变化，检查是否有画面异常或系统不稳定。

**警告**：电压调整可能导致系统不稳定或 GPU 损坏，请谨慎操作。建议先从较小的电压偏移开始。

**要求**：
- 提供完整的电压调整命令序列。
- 记录电压降低前后的功耗数据。
- 描述稳定性测试的结果。

**关键命令**：
```bash
# 查看当前 V/F 曲线
cat /sys/class/drm/card0/device/pp_od_clk_voltage

# 设置电压偏移（格式：v 频率 电压）
echo "v 2000 1050" > /sys/class/drm/card0/device/pp_od_clk_voltage
echo "c" > /sys/class/drm/card0/device/pp_od_clk_voltage

# 稳定性测试
timeout 300 glmark2 --run-forever
```

### 任务 2.5：综合监控与日志分析（6 分）

**目标**：使用内核调试工具观察频率‑功耗‑温度三者的动态交互。

**步骤**：
1. 使用 `ftrace` 或 `perf` 跟踪 amdgpu 电源管理相关函数的调用。
2. 在调整频率和功耗限制时，观察内核日志中的相关消息。
3. 创建一个时间同步的监控日志，包含时间戳、频率、功耗、温度。

**要求**：
- 提供一个综合监控脚本，每秒记录所有关键指标。
- 使用 `dmesg -w` 或 `journalctl -f` 观察内核消息。
- 分析频率调整与功耗变化之间的延迟。

**综合监控脚本示例**：
```bash
#!/bin/bash
logfile="gpu_monitor_$(date +%Y%m%d_%H%M%S).csv"
echo "timestamp,sclk(mhz),power(w),temp(c),dpm_state" > $logfile

while true; do
    timestamp=$(date +%Y-%m-%d_%H:%M:%S.%3N)
    sclk=$(cat /sys/class/drm/card0/device/pp_dpm_sclk | grep "*" | awk '{print $2}')
    power=$(cat /sys/class/drm/card0/device/hwmon/hwmon*/power1_input)
    temp=$(cat /sys/class/drm/card0/device/hwmon/hwmon*/temp1_input)
    dpm_state=$(cat /sys/class/drm/card0/device/power_dpm_state)
    
    echo "$timestamp,$sclk,$((power/1000000)),$((temp/1000)),$dpm_state" >> $logfile
    sleep 1
done
```

---

## 第三部分：综合实验设计（40 分）

### 实验目标
设计并执行一个完整的实验，探索 **频率‑电压‑功耗** 三者的权衡关系，找到给定工作负载下的最优能效点。

### 实验要求
1. **工作负载选择**：选择一个可重复的 GPU 负载（如 `glmark2`、`vkmark`、`clpeak` 或自定义的 OpenCL/HIP 内核）。
2. **控制变量**：
   - 独立变量：SCLK 频率（通过性能等级或固定频率控制）
   - 独立变量：核心电压（通过 undervolting 调整）
   - 独立变量：PPT 限制
3. **测量指标**：
   - 性能指标：FPS、GFLOPS、任务完成时间等
   - 功耗指标：平均功耗、峰值功耗
   - 能效指标：性能/功耗比
   - 温度指标：最高温度、平均温度
4. **实验设计**：至少包含 3×3×2 = 18 个测试点（3 个频率等级 × 3 个电压设置 × 2 个 PPT 限制）。

### 实验步骤设计

**步骤 1：环境准备**
1. 确保系统无其他 GPU 负载。
2. 设置监控脚本，记录所有关键指标。
3. 准备测试工作负载，确保可重复执行。

**步骤 2：基准测试**
1. 在默认设置下运行工作负载，记录基准性能。
2. 测量默认的 V/F 曲线和 PPT 限制。

**步骤 3：频率扫描测试**
1. 固定电压为默认值，PPT 为默认值。
2. 依次测试不同的 SCLK 等级（低、中、高）。
3. 记录每个频率点的性能和功耗。

**步骤 4：电压扫描测试**
1. 固定频率为中间等级，PPT 为默认值。
2. 依次测试不同的电压设置（默认、-50mV、-100mV）。
3. 记录每个电压点的性能、功耗和稳定性。

**步骤 5：PPT 限制测试**
1. 固定频率和电压为中间值。
2. 测试不同的 PPT 限制（默认、默认的 80%、默认的 120%）。
3. 观察 PPT 限制对实际功耗和性能的影响。

**步骤 6：综合分析**
1. 计算每个测试点的能效比（性能/功耗）。
2. 找出能效最高的配置组合。
3. 分析各因素对性能、功耗、能效的影响权重。

### 实验报告要求

请提交一份包含以下内容的实验报告：

**1. 实验环境描述**
```
- GPU 型号：
- 驱动版本：
- 内核版本：
- ROCm 版本：
- 测试负载：
- 监控工具：
```

**2. 数据收集表格**

设计一个表格记录所有测试点的数据，例如：

| 测试ID | SCLK等级 | 电压偏移 | PPT限制 | 性能得分 | 平均功耗 | 峰值功耗 | 最高温度 | 能效比 | 稳定性 |
|--------|----------|----------|---------|----------|----------|----------|----------|--------|--------|
| 1      | 2 (低)   | 0 mV     | 默认    |          |          |          |          |        |        |
| 2      | 4 (中)   | 0 mV     | 默认    |          |          |          |          |        |        |
| ...    | ...      | ...      | ...     | ...      | ...      | ...      | ...      | ...    | ...    |

**3. 结果可视化**

提供至少两张图表：
1. **频率‑性能‑功耗关系图**：X 轴为频率，左 Y 轴为性能，右 Y 轴为功耗。
2. **电压‑功耗‑能效关系图**：X 轴为电压偏移，显示功耗和能效的变化。

**4. 分析结论**

回答以下问题：
1. 在您的测试中，哪个因素（频率、电压、PPT）对功耗的影响最大？
2. 哪个因素对性能的影响最大？
3. 是否存在一个"甜点"配置，在性能损失很小的情况下显著降低功耗？
4. 您的 undervolting 尝试是否成功？电压最多能降低多少仍保持稳定？
5. PPT 限制是否有效控制了最大功耗？在哪些情况下可能被突破？

**5. 优化建议**

基于实验结果，为以下场景提供配置建议：
1. **最大化性能**：不考虑功耗，追求最高性能。
2. **最大化能效**：在性能损失不超过 10% 的情况下，追求最佳能效比。
3. **静音运行**：限制功耗以降低温度和风扇噪音。

### 评分标准
- **实验设计完整性**：10 分（控制变量合理、测试点充足）
- **数据收集准确性**：10 分（数据全面、记录规范）
- **分析深度**：10 分（分析透彻、图表清晰）
- **结论实用性**：10 分（建议具体、可操作）

---

## 第四部分：内核代码分析（10 分）

### 分析任务
跟踪一次频率调整请求从用户空间到硬件执行的全过程，分析内核中的关键函数调用链。

### 代码分析要求

**步骤 1：确定入口点**
当通过 sysfs 写入 `pp_od_clk_voltage` 文件请求频率调整时，找到处理该请求的内核函数。

**提示**：在 `amdgpu` 驱动中搜索 `pp_od_clk_voltage` 相关的属性定义。

**步骤 2：分析函数调用链**
从 sysfs 写入处理函数开始，跟踪到最终硬件寄存器写入。至少找出以下关键函数：

1. Sysfs 写入处理函数
2. 参数解析与验证函数
3. SMU（System Management Unit）消息构造函数
4. SMU 消息发送函数
5. 硬件寄存器写入函数

**步骤 3：绘制调用流程图**
使用 ASCII 艺术风格绘制简化的调用流程图：

```
用户空间写入 pp_od_clk_voltage
       │
       ▼
amdgpu_device_attr_store()           // drivers/gpu/drm/amd/amdgpu/amdgpu_device.c
       │
       ▼
amdgpu_dpm_set_pp_od_clk_voltage()   // drivers/gpu/drm/amd/pm/amdgpu_dpm.c
       │
       ▼
smu_set_od_clk_voltage()             // drivers/gpu/drm/amd/pm/swsmu/smu.c
       │
       ▼
smu_v13_0_set_od_clk_voltage()       // drivers/gpu/drm/amd/pm/swsmu/smu_v13_0.c
       │
       ▼
smu_cmn_send_smc_msg_with_param()    // drivers/gpu/drm/amd/pm/swsmu/smu_cmn.c
       │
       ▼
amdgpu_asic_send_smc_msg()           // drivers/gpu/drm/amd/amdgpu/amdgpu_asic.c
       │
       ▼
硬件寄存器写入（具体函数取决于 ASIC 类型）
       │
       ▼
SMU 固件处理请求，调整频率/电压
```

**步骤 4：关键代码片段分析**
从内核源码中找出以下关键代码片段，并解释其作用：

1. **参数解析代码**：如何解析用户输入的 "s 5" 或 "v 2000 1050" 这样的命令？
2. **边界检查代码**：如何确保用户请求的频率/电压在安全范围内？
3. **SMU 消息格式**：频率/电压调整请求的 SMU 消息结构是怎样的？

**示例代码查找**：
```bash
# 在源码中搜索相关函数
grep -r "pp_od_clk_voltage" drivers/gpu/drm/amd/
grep -r "set_od_clk_voltage" drivers/gpu/drm/amd/
```

**步骤 5：同步机制分析**
分析当多个用户空间进程同时尝试调整频率时，内核如何保证操作的原子性和一致性。

**要求**：
- 找出相关的锁机制（mutex、spinlock 等）。
- 分析是否存在竞争条件的风险。

### 评分标准
- **调用链完整性**：4 分（找到所有关键函数）
- **流程图准确性**：3 分（流程正确、层次清晰）
- **代码分析深度**：3 分（至少分析一个关键代码片段）

---

## 考核提交要求

1. **答案格式**：创建一个名为 `305_考核_答案_<你的姓名>.md` 的 Markdown 文档。
2. **内容要求**：
   - 第一部分：选择题答案（只需提供选项，无需解释）
   - 第二部分：每个任务的完整命令序列和输出结果
   - 第三部分：完整的实验报告
   - 第四部分：内核代码分析结果
3. **提交方式**：将文档放置于项目目录中。
4. **截止时间**：自开始考核起 4 小时内完成。

## 参考答案与评分要点

### 第一部分参考答案
**1.1 单选题**：1-C, 2-B, 3-C, 4-C, 5-B  
**1.2 多选题**：6-ABC, 7-ABCD, 8-ABCD, 9-ABCE, 10-ABD

### 第二部分评分要点
- **任务 2.1**：命令完整、输出解释清楚、数据一致性验证（6 分）
- **任务 2.2**：频率调整成功、功耗变化记录、分析合理（6 分）
- **任务 2.3**：功耗限制设置正确、监控有效、分析透彻（6 分）
- **任务 2.4**：电压调整方法正确、稳定性测试充分（6 分）
- **任务 2.5**：监控脚本完整、日志分析深入（6 分）

### 第三部分评分要点
- 实验设计科学合理（10 分）
- 数据收集全面准确（10 分）
- 分析深入且有图表支持（10 分）
- 结论实用性强（10 分）

### 第四部分评分要点
- 函数调用链完整正确（4 分）
- 流程图清晰准确（3 分）
- 代码分析有深度（3 分）

## 考核通过标准
- **总分 ≥ 70 分**：通过考核，可以进入第 306 天的学习。
- **总分 50‑69 分**：需要复习第 302‑304 天的内容，并在 24 小时内补考。
- **总分 < 50 分**：需要重新学习第 302‑304 天的内容，3 天后重新考核。

---

## 扩展资源

### 内核源码参考
- `drivers/gpu/drm/amd/pm/amdgpu_dpm.c`：电源管理核心逻辑
- `drivers/gpu/drm/amd/pm/swsmu/smu_v13_0.c`：RDNA 3 SMU 实现
- `drivers/gpu/drm/amd/amdgpu/amdgpu_device.c`：设备属性与 sysfs 接口
- `drivers/gpu/drm/amd/pm/inc/smu13_driver_if_v13_0.h`：SMU 消息定义

### 工具参考
- **rocm‑smi 官方文档**：https://rocm.docs.amd.com/projects/amdsmi/en/latest/
- **Phoronix Test Suite**：自动化性能测试框架
- **glmark2**：开源 OpenGL 基准测试
- **clpeak**：OpenCL 基准测试工具

### 调试技巧
1. **内核日志过滤**：`dmesg | grep -E "amdgpu|smu|dpm"`
2. **实时监控**：使用 `watch -n 1` 结合 rocm‑smi 或 sysfs 查询
3. **性能分析**：使用 `perf` 跟踪内核函数调用频率
4. **回归测试**：每次只调整一个变量，记录基准结果

---

## 今日总结

本考核综合了第 302‑304 天关于 GPU 频率、电压、功耗管理的所有知识点，通过：
1. **理论知识测验**检验概念掌握程度
2. **实践操作任务**验证命令行操作能力
3. **综合实验设计**培养系统性实验能力
4. **内核代码分析**深入理解实现原理

通过本次考核，您应能：
- 熟练使用 sysfs、debugfs、rocm‑smi 监控和调整 GPU 性能状态
- 设计并执行频率‑电压‑功耗权衡实验
- 理解内核中电源管理的实现框架
- 具备实际 GPU 性能调试和优化的能力

**下一日预告**：第 306 天将学习 **"温度监控：热传感器与温度阈值"**，深入了解 GPU 温度监测机制、热传感器类型、温度阈值设置及热保护策略。